# twitter-gemini-autopilot

Small project — autopilot helpers for Twitter + Gemini automation.

How to push this repo to GitHub:

- Option A (provide remote URL):
  - Create a repo on GitHub, then run:
    - `git remote add origin <REMOTE_URL>`
    - `git branch -M main`
    - `git push -u origin main`

- Option B (give me a Personal Access Token):
  - I can create the GitHub repo via the API and push if you provide a PAT with `repo` scope.

Files added locally: `main.py`, `fix.py`, `requirements.txt`.
